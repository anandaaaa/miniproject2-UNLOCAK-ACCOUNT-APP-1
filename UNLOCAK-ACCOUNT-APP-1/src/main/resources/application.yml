unlock-app:
  messages:
    userAccunLockSuccess: User Account Unlocked successfully
    inValidPwd: Invalid Temp Password

server:
  port: 9092
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: admin
    url: jdbc:mysql://localhost:3306/login
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    

  